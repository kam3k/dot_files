# set mod key
set $mod Mod1

# set terminal
set $term urxvt

# load xresources
exec_always --no-startup-id xrdb -load ~/.Xresources 

# font
font pango:Iosevka Term 8

# hide title bars
new_window 1pixel

# start a terminal
bindsym $mod+Return exec $term

# kill focused window
bindsym $mod+q kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+z split v

# split in vertical orientation
bindsym $mod+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# show the scratchpad window
bindsym $mod+minus [instance="term_scratch"] scratchpad show

# set default container layout (default, stacking, or tabbed)
workspace_layout default

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacked
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+f floating toggle
floating_modifier $mod

set $workspace1 "1"
set $workspace2 "2"
set $workspace3 "3"
set $workspace4 "4"
set $workspace5 "5"
set $workspace6 "6"
set $workspace7 "7"
set $workspace8 "8"
set $workspace9 "9"
set $workspace10 "10"

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace10
bindsym $mod+p workspace back_and_forth

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10

# start scratchpad
for_window [instance="term_scratch"] move scratchpad
exec --no-startup-id $term -name term_scratch
bindsym $mod+n exec $term -name term_scratch

# applications that should always float
floating_minimum_size 75 x 50
floating_maximum_size 2000 x 1600

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# move workspace to other monitor
bindsym $mod+m move workspace to output right

# set colors
set $black #373B41
set $red #CC6666
set $green #B5BD68
set $yellow #F0C674
set $blue #81A2BE
set $magenta #B294BB
set $cyan #8ABEB7
set $white #C5C8C6
set $bg-color #1D1F21
set $lighter-bg-color #2D3033
set $lightest-bg-color #3d4145
set $inactive-bg-color #282A36
set $text-color #282A2E
set $inactive-text-color #707880

# borders
hide_edge_borders smart # no borders when only one window
# class                 border      backgr.     text         indicator child_border
client.focused          $magenta    $magenta    $bg-color    $green    $magenta
client.focused_inactive $magenta    $magenta    $bg-color    $green    $bg-color
client.unfocused        $magenta    $magenta    $bg-color    $green    $bg-color
client.urgent           $red        $red        $bg-color    $green    $red

# bar configuration 
bar {
    position top
    tray_output none
    status_command python3 ~/.local/src/bumblebee-status/bumblebee-status \
                   -m weather disk battery cpu pasink brightness date time \
                   -p time.format="%H:%M" disk.format="{left}" battery.warning=30 battery.critical=15 cpu.left-click="urxvt -e htop" disk.left-click="urxvt -e ncdu" date.left-click="gsimplecal" brightness.step=5 \
                   -t hybrid-powerline 

    colors {
        background $bg-color
        separator $bg-color
        #                   border              background          text
        focused_workspace   $lightest-bg-color  $lightest-bg-color  $white
        inactive_workspace  $bg-color           $bg-color           $lightest-bg-color
        active_workspace    $bg-color           $bg-color           $white
        urgent_workspace    $red                $red                $text-color
    }
}

# Place gsimplecal in a nice spot when you click the calendar
for_window [class="(?i)gsimplecal"] floating enable, move position mouse, move down 30 px

# resize windows
mode "resize" {
    bindsym h resize shrink width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym l resize grow width 5 px or 5 ppt

    # press enter or escape to go back to normal
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# rofi
bindsym $mod+space exec --no-startup-id "rofi -show drun -theme hybrid"

# lock screen
bindsym $mod+Shift+x exec i3lock

# pulse audio controls
bindsym XF86AudioRaiseVolume exec pactl -- set-sink-volume @DEFAULT_SINK@ +8%; exec pactl -- set-sink-mute @DEFAULT_SINK@ 0
bindsym XF86AudioLowerVolume exec pactl -- set-sink-volume @DEFAULT_SINK@ -8%; exec pactl -- set-sink-mute @DEFAULT_SINK@ 0
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle

# use dunst for notifications
exec_always killall -q notify-osd
exec_always --no-startup-id dunst

# set wallpaper
exec --no-startup-id feh --bg-scale ~/.config/wallpapers/outrun_car.png

# tap-to-click on trackpad
exec --no-startup-id xinput set-prop "Elan Touchpad" "libinput Tapping Enabled" 1

# disable touchscreen
exec --no-startup-id xinput --disable "ELAN Touchscreen"
